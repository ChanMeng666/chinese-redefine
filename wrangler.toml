account_id = "c87dca24333f7ed5d643f731f6308fec"
name = "chinese-redefine"
main = ".open-next/worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

assets = { directory = ".open-next/assets", binding = "ASSETS" }

# [vars]
# Non-secret environment variables can be added here.
# Secrets should be set via `wrangler secret put <KEY>` instead.
# DATABASE_URL, BETTER_AUTH_SECRET, etc. should be set as secrets.
